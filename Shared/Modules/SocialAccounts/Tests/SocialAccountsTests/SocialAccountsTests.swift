import XCTest
@testable import SocialAccounts

final class SocialAccountsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SocialAccounts().text, "Hello, World!")
    }
}
